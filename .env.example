POSTGRES_DB=app_db
POSTGRES_USER=app_user
POSTGRES_PASSWORD=change_me
COMPOSE_PROJECT_NAME=local-llm

# Open WebUI authenticates to LiteLLM with this key. Set it to the LiteLLM
# master key unless you intentionally use a different virtual key.
OPENWEBUI_API_KEY=sk-your-master-key
OPENWEBUI_DEFAULT_MODEL=qwen-3.5-9b
OPENWEBUI_TASK_MODEL=qwen-3.5-9b-task
OPENWEBUI_DB_NAME=openwebui_db
OPENWEBUI_DB_USER=openwebui_user
OPENWEBUI_DB_PASSWORD=change_me
OPENWEBUI_DATABASE_URL=postgresql://${OPENWEBUI_DB_USER}:${OPENWEBUI_DB_PASSWORD}@postgres:5432/${OPENWEBUI_DB_NAME}

# Only needed if you uncomment a cloud model in configs/litellm.yaml.
OPENAI_API_KEY=sk-your-provider-key
# Backend auth between LiteLLM and the local vLLM server.
LITELLM_API_KEY=sk-your-backend-key
LITELLM_MASTER_KEY=sk-your-master-key
LITELLM_DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB}

# Daily report
DISCORD_WEBHOOK_URL=

# Model-specific settings (adjust based on model capabilities)
VLLM_SERVED_MODEL=Qwen/Qwen3.5-9B
LITELLM_MODEL_NAME=qwen-3.5-9b
MODEL_MAX_CONTEXT_LEN=262144
MODEL_DEFAULT_MAX_TOKENS=4096
MODEL_DEFAULT_TEMPERATURE=0.6
MODEL_DEFAULT_TOP_P=0.95
TASK_MODEL_MAX_TOKENS=128
TASK_MODEL_TEMPERATURE=0.2
TASK_MODEL_TOP_P=0.9

# vLLM runtime parameters
VLLM_GPU_MEMORY_UTILIZATION=0.85
VLLM_MAX_NUM_SEQS=16
VLLM_TOOL_CALL_PARSER=qwen3_coder
VLLM_REASONING_PARSER=qwen3
